name: 📚 Daily arXiv Digest → Notion (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest

    steps:
      - name: Notion digest disabled
        run: |
          echo "Notion-based arXiv digest is disabled."
          echo "Use Sveltia CMS at /admin for browser-based paper review publishing."
