name: 📝 Blog Publisher (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest

    steps:
      - name: Notion publisher disabled
        run: |
          echo "Notion-based blog publishing is disabled."
          echo "Use Sveltia CMS at /admin to write and publish posts."
