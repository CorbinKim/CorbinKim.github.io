name: 📊 Weekly Review → Notion (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest

    steps:
      - name: Notion weekly review disabled
        run: |
          echo "Notion-based weekly review is disabled."
          echo "Obsidian remains private notes only; blog publishing happens through /admin."
