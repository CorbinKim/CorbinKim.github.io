name: 📊 Citation Tracker → Notion (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest

    steps:
      - name: Notion citation tracker disabled
        run: |
          echo "Notion-based citation tracker is disabled."
          echo "No NOTION_TOKEN-dependent workflow runs automatically."
